import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

import WouldYouRather from './app/App';

export default WouldYouRather;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center'
  }
});
